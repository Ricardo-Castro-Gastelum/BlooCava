[package]
name = "bloocava"
version = "0.1.0"
edition = "2021"
description = "A terminal UI for configuring Cava audio visualizer colors"
authors = ["Ricky"]

[dependencies]
crossterm = "0.28"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
dirs = "6"
